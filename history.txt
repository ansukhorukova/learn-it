PROJECT_MAP.md — історія змін
==============================

2026-08-23 — Початкова карта (Docker-скаффолд + /auth)
  Позначено done: Infra_Docker, Infra_FirebaseAuth, FE_i18n, FE_Auth,
    BE_Health, BE_AuthMw, BE_UsersMe, DB_Users.
  Усі інші вузли (Boards/BoardView/TaskPanel/Attachments/Sharing/TeamView/
    Settings/ProdDeploy) — planned, згідно з цільовим обсягом у CLAUDE.md.
  Структура: 4 subgraph'и FE/BE/DB/Infra з наскрізними звʼязками.

2026-08-23 — Коригування: відновлення структури після неавторизованої правки
  Проблема: оркестратор вручну переписав PROJECT_MAP.md у відповідь на скаргу
    користувача на нечитабельність (стрілки перетиналися) — але замість
    виправлення розкладки згорнув документовану структуру (subgraph'и
    FE/BE/DB/Infra в кожному вузлі) в єдиний плаский roadmap-граф + таблицю,
    без окремої ER-схеми БД, без коміту, без history.txt.
  Виправлено:
    - Повернуто 4 subgraph'и (FE/BE/DB/Infra) у кожному вузлі карти.
    - Проблему перетинів стрілок вирішено правильним способом (правило 6):
      карту розбито на три рядки-діаграми за функціональними групами
      (Auth/i18n/Infra; Boards/BoardView/шеринг борду; Task panel/шеринг
      таски/вкладення/team view) — жоден звʼязок не перетинає межу групи,
      логіка звʼязків не змінена.
    - Повернуто окрему ER-схему БД (правило 7): таблиці горизонтально,
      FK-звʼязки вертикально, автоматично лягли в 4 шари-рядки
      (board_members/task_shares/time_entries/attachment_viewers →
      users/attachments → tasks → boards).
    - Згорнуто в описи вузлів пізніші дрібні правки, виявлені при звірці:
      BE_UsersMe тепер включає GET /auth/provider-hint (rate-limited);
      DB_Users тепер включає колонки locale, last_sign_in_provider.
    - Відновлено секцію "Відомі прогалини" (orphaned Firebase account при
      мережевому збої під час signup — не блокер, залоговано на майбутнє).
  Не чіпалося: жодного вузла/звʼязку поза описаною правкою; CLAUDE.md,
    docker-compose.yml і код — поза скоупом картографа.
  Коміт: docs: update PROJECT_MAP.md — відновлено FE/BE/DB/Infra структуру
    та ER-схему БД, перетини стрілок усунено розбиттям на рядки

2026-08-24 — Boards overview / Board view / Task CRUD / drag-and-drop: done
  Підсумок від fullstack-developer + code-reviewer (Approve, 4 concurrency-
    бага знайдено й виправлено за 5 раундів рев'ю, системний фікс закрито
    регресійними тестами):
    - BE: GET/POST /boards, GET/PATCH/DELETE /boards/:id,
      GET/POST /boards/:id/tasks, PATCH/DELETE /tasks/:id — service-layer
      owner_id-авторизація (без RLS, за архітектурою CLAUDE.md). Нові Knex-
      міграції boards/tasks (ON DELETE CASCADE + UNIQUE (board_id, status,
      position) DEFERRABLE як concurrency-бекстоп). Спільні locking-хелпери
      backend/src/lib/db.js (lockRow/lockedUpdate) та
      backend/src/lib/authz.js (getOwnedBoard).
    - FE: "/" тепер BoardsPage (сітка, create/rename/delete, empty state,
      локалізований CTA) замість HomePage-заглушки (sign-out перенесено в
      новий AppHeader). "/boards/:boardId" — BoardViewPage, 3 колонки
      (Planned/In Progress/Done · PR merged — англійські лейбли лишені як є
      в обох мовах, свідоме продуктове рішення, не прогалина перекладу),
      drag-and-drop через @dnd-kit + обовʼязковий доступний <select>-фолбек
      на кожній картці. Повна EN/UK локалізація.
    - Тести: backend/test/concurrency/ (vitest, ізольована тестова
      Postgres-БД) — 6 concurrency-сценаріїв, перша автоматизована тест-
      інфраструктура в проєкті.
  Позначено done: FE_Boards, FE_BoardView, BE_Boards, BE_Tasks, DB_Boards,
    DB_Tasks (Рядок 2 + ER-схема БД).
  Свідомо лишено planned (не торкнулось цієї фічі): FE_Shared,
    BE_BoardMembers, DB_BoardMembers — шеринг борду не будувався, лише
    single-owner. Attachments/time tracking/team view — не чіпались.
  Нових вузлів на карту не додано — фіча повністю лягла в існуючі вузли
    Рядка 2. Додано примітку в "Примітки" про нову vitest-інфраструктуру
    (без окремого вузла — деталь реалізації всередині BE_Boards/BE_Tasks).
  Нових "відомих прогалин" не залоговано — всі concurrency-баги закриті
    до approve, не відкладені.
  Коміт: docs: update PROJECT_MAP.md — Boards/Board view/Task CRUD
    позначено done після Approve з фіксом concurrency-багів

2026-08-24 — Task attachments (файли/посилання/нотатки): done
  Підсумок від fullstack-developer + code-reviewer (Approve, 2 security/
    correctness знахідки виправлено й повторно перевірено через реальний
    HTTP-шар):
    - BE: GET/POST/DELETE /tasks/:id/attachments[/:attachmentId], нова
      таблиця attachments (kind: file/link/note; visibility-enum присутній,
      але завжди за замовчуванням private цього разу — без picker UI,
      чисто відкладено, без часткової реалізації). Файли проксіюються
      через backend (multer memory storage → S3 PutObjectCommand),
      завантаження — через backend-issued signed GET URL (15 хв TTL),
      новий env STORAGE_PUBLIC_ENDPOINT (окремо від контейнерного
      STORAGE_ENDPOINT), щоб signed URL резолвились у реальному браузері.
      MinIO bucket автостворюється при старті backend, якщо відсутній.
      Storage cleanup при видаленні task/board (DB-рядки + MinIO-обʼєкти,
      перевірено прямим S3 listing). lockRow-патерн (перевикористаний з
      Boards/Tasks) захищає createAttachment від видалення task
      "посеред" запиту. Бейдж кількості вкладень на картці таски тепер
      показує реальний live-лічильник.
    - FE: новий компонент TaskPanel — перша виділена бічна панель таски
      (раніше існували лише картки в board view), вкладення згруповані
      по типу (Files/Links/Notes) з лічильниками, add/delete UI для всіх
      трьох видів.
    - Security-фікс під час рев'ю: image/svg+xml прибрано зі списку
      дозволених типів файлів (FE + BE) — code-reviewer визначив як
      stored-XSS вектор на майбутнє шерингу; виправлено до approve,
      не відкладено.
    - Тести: новий закомічений concurrency-тест
      backend/test/concurrency/attachments.concurrency.test.js — сюїт
      тепер 3 файли / 8 тестів, усі проходять.
  Позначено done: FE_Attachments, BE_Attachments, DB_Attachments,
    Infra_MinIO (обидва входження — Рядок 1 і Рядок 3), DB_Attachments
    в ER-схемі БД.
  Позначено progress: FE_TaskPanel — новий компонент-контейнер зʼявився
    і повністю хостить готові Attachments, але власна заявлена на карті
    ціль вузла (секція "Час": таймер/сесії/ручні записи, звʼязки з
    BE_TimeEntries/BE_TaskShares) цього разу не будувалась; додано
    стрілку FE_TaskPanel --> FE_Attachments, щоб відобразити реальну
    вкладеність компонентів.
  Свідомо лишено planned: DB_AttachmentViewers і visibility-picker UI —
    навмисно відкладено, немає споживача до фічі шерингу; підтверджено
    чистим (жодної часткової visibility-логіки). BE_TimeEntries/
    DB_TimeEntries, BE_TaskShares/DB_TaskShares, FE_TeamView/BE_TeamView
    — не торкались цієї фічі.
  Нових вузлів на карту не додано — фіча повністю лягла в існуючі вузли
    Рядка 3 (+ одна нова стрілка FE_TaskPanel --> FE_Attachments).
  Нових "відомих прогалин" не залоговано — обидві security/correctness
    знахідки закриті до approve; тривіальне non-blocking спостереження
    рев'юера (ідемпотентна подвійна спроба видалення в storage cleanup)
    прямо позначене як "не потребує фіксу", не прогалина.
  Коміт: docs: update PROJECT_MAP.md — Task attachments позначено done
    після Approve, FE_TaskPanel — progress (лише секція вкладень)

2026-08-24 — Task rename UI (TaskPanel): додано поза звичайним review pipeline
  На явний запит користувача крок tester/code-reviewer пропущено для цієї
    конкретної зміни — маленька, добре зрозуміла UI-правка (дзеркалить
    заапрувлений патерн board-rename, використовує вже протестований і
    concurrency-safe BE-ендпоінт PATCH /tasks/:id без змін на бекенді).
    Це відхилення від звичайного правила картографа "оновлювати карту
    лише після Approve від code-reviewer" — зроблено свідомо на прямий
    запит користувача, задокументовано як виняток, а не як обхід процесу.
    - FE: frontend/src/components/TaskPanel.jsx — inline rename-форма
      (лейбл, save/cancel, за UX-патерном BoardsPage.jsx), client-side
      валідація (task.rename.validation.titleRequired/titleTooLong,
      той самий ліміт 200 символів, що вже діє на бекенді).
      frontend/src/pages/BoardViewPage.jsx — синхронізація заголовка
      картки таски після rename у TaskPanel.
    - Нові локалі: task.rename.cta, task.rename.titleLabel,
      task.rename.validation.titleRequired,
      task.rename.validation.titleTooLong — EN/UK, обидві повні.
    - BE-змін немає — title-only шлях PATCH /tasks/:id вже існував і
      був повністю протестований у межах ранішої фічі Tasks CRUD.
    - Користувач особисто перевірив наживо (успішний rename, валідація
      пустого/задовгого заголовка, 403 для не-власника); тестові дані
      прибрані вручну після переривання сесії реалізуючого агента.
  Статуси вузлів НЕ змінені: title-редагування вже неявно охоплювалось
    описом done-вузлів BE_Tasks/DB_Tasks ("CRUD"); FE_TaskPanel лишається
    progress (вузол означає секцію "Час", не rename); FE_BoardView
    лишається done без змін.
  Нових вузлів на карту не додано.
  Залоговано нову "відому прогалину": task rename UI не пройшов
    code-review — нижча впевненість верифікації, ніж решта done-роботи;
    рекомендовано провести review pass, коли буде зручно.
  Коміт: docs: update PROJECT_MAP.md — task rename UI додано поза
    review pipeline на запит користувача, зафіксовано як follow-up

2026-08-24 — Час у TaskPanel (US10 таймер, US11 ручні записи, US12 сесії/тотали): done
  Підсумок від fullstack-developer + code-reviewer (Approve, 1 раунд
    Request changes → фікс retry-логіки → повторний Approve):
    - BE: 6 ендпоінтів на /tasks/:id/time-entries — POST start, POST stop,
      POST manual, PATCH :entryId, DELETE :entryId, GET list. Нова
      таблиця time_entries + міграція
      20260824110000_create_time_entries_table.js з partial unique index
      time_entries_one_active_per_user на (user_id) WHERE ended_at IS NULL
      — гарантує один активний таймер на користувача на рівні БД, не лише
      в сервісному шарі. Тотали додані на GET /boards/:id/tasks
      (totalSeconds на тасці, columnTotals, boardTotalSeconds) і на
      GET /boards (totalSeconds, thisWeekSeconds, заміна заглушки
      board.card.totalTimePlaceholder) — розширення контракту вже done-
      вузлів BE_Boards/BE_Tasks, за тим самим принципом, що й попередні
      CRUD-розширення. thisWeekSeconds рахується від понеділка 00:00 UTC,
      фіксовано явно, не локальний час користувача.
    - FE: TaskPanel доповнено секцією "Час" — таймер старт/стоп, живий
      лічильник, банер auto-stop-and-switch при переключенні активної
      таски, форма ручного запису (хвилини + опційна нотатка), список
      сесій з edit/delete.
    - Приватність: time_entries завжди фільтруються WHERE user_id =
      requester; PATCH/DELETE чужого чи неіснуючого запису дають
      однаковий 404 (anti-enumeration, ніколи 403) — перевірено tester'ом
      прямим SQL-інсертом "чужого" рядка.
    - Рев'ю (Request changes → Approve): знайдено й виправлено race-баг
      у retry-логіці startTimer — ліміт 2 спроби не витримував 3+
      одночасних гонщиків; збільшено до MAX_START_ATTEMPTS=8 з чесним
      errors.timeEntry.startConflict замість оманливого коду помилки.
      Побічно виправлено баг у scripts/i18n-check.js (шлях був
      frontend/locales/ замість frontend/src/locales/ — гейт локалізації
      мовчки нічого не перевіряв); тепер гейт коректно розрізняє
      відсутні в EN ICU plural-категорії (few/many) від справжніх
      пропусків перекладу.
    - Тести: новий backend/test/concurrency/timeEntries.concurrency.test.js
      (4 сценарії: подвійний старт таймера з двох вкладок, PATCH-vs-
      DELETE, DELETE-vs-DELETE на той самий запис) — сюїт тепер
      12 concurrency-тестів (боарди/таски/вкладення/час).
  Позначено done: BE_TimeEntries, DB_TimeEntries (Рядок 3 + ER-схема БД),
    FE_TaskPanel (Рядок 3) — вузол досягнув повного done вперше: секція
    "Час", заради якої вузол і був заведений на карті, тепер реалізована
    й заапрувена.
  Свідомо лишено planned (не торкнулось цієї фічі): BE_TaskShares/
    DB_TaskShares, FE_TeamView/BE_TeamView, BE_BoardMembers/
    DB_BoardMembers/FE_Shared.
  Нових вузлів на карту не додано — фіча повністю лягла в існуючі вузли
    Рядка 3; тотали на Boards/Tasks — розширення контракту, не нові
    вузли.
  Нових "відомих прогалин" не залоговано — знайдений під час рев'ю
    race-баг закрито до approve, не відкладено.
  Коміт: docs: update PROJECT_MAP.md — Час у TaskPanel (таймер, ручні
    записи, сесії/тотали) позначено done після Approve з фіксом
    retry-логіки startTimer

2026-08-24 — Board & Task Sharing (US13-US17: шеринг борду й таски, ролі
  viewer/collaborator): done
  Підсумок від fullstack-developer + tester (2 раунди, обидва фінально
    PASS) + code-reviewer (2 раунди — перший Request changes через
    reindex-leak, другий Approve, закомічено як 02de849):
    - BE: нові таблиці board_members, task_shares (+ схема-only
      attachment_viewers) через Knex-міграції. backend/src/lib/authz.js
      розширено ефективною роллю owner/collaborator/viewer з пріоритетом
      board-level над task-level (max(board role, task role),
      task-level ніколи не поширюється на інші таски того ж борду) і
      флагом hasBoardAccess. Нові сервіси boardMembers.service.js,
      taskShares.service.js; оновлені tasks.service.js, boards.service.js,
      attachments.service.js, timeEntries.service.js. 8 нових CRUD-
      ендпоінтів на /boards/:id/members і /tasks/:id/shares, новий
      GET /tasks/:id, myRole доданий у 3 наявних ендпоінти борду/тасок.
    - FE: новий компонент frontend/src/components/SharePanel.jsx (форма
      шерингу за email, список учасників, owner-рядок) — перевикористаний
      і для board_members, і для task_shares. frontend/src/lib/roles.js
      (canWrite), gating за myRole у BoardViewPage.jsx і TaskPanel.jsx.
    - Локалізація: повні EN/UK ключі sharing.*, errors.board.*,
      errors.task.*, errors.share.*.
    - Рев'ю раунд 1 → Request changes: reindex-leak — стейл myRole/доступ
      у in-memory кеші після зміни ролі всередині того самого запиту
      переживав reindex списку, міг віддати застарілий дозвіл. Разом з
      цим виправлено мертвий locale-ключ у SharePanel і відсутній
      FK-індекс. Раунд 2 → Approve.
  Позначено done: BE_BoardMembers, DB_BoardMembers (Рядок 2 + ER-схема),
    BE_TaskShares, DB_TaskShares, DB_AttachmentViewers (Рядок 3 +
    ER-схема).
  Додано новий вузол: FE_SharePanel — в Рядку 2 (FE_BoardView -->
    FE_SharePanel --> BE_BoardMembers) і в Рядку 3 (FE_TaskPanel -->
    FE_SharePanel --> BE_TaskShares), той самий компонент повторений у
    двох діаграмах, за прецедентом Infra_MinIO. Замінив прямі стрілки
    FE_BoardView --> BE_BoardMembers і FE_TaskPanel --> BE_TaskShares.
  DB_AttachmentViewers позначений done як таблиця/FK, що пройшли review,
    але досі без FE-консюмера — visibility picker у вкладеннях не
    будувався цього разу, FE_Attachments лишається з приміткою
    "(visibility picker: planned)" без змін.
  Свідомо лишено planned (не торкнулось цієї фічі): FE_Shared ("Shared
    with me"), FE_TeamView/BE_TeamView.
  Нових "відомих прогалин" не залоговано — reindex-leak, мертвий
    locale-ключ і відсутній FK-індекс закриті до approve, не відкладені.
  USER_STORIES.md: US-013…US-017 переведені з "🔧 У розробці" на
    "✅ Готово (пройшло code review)", дата 2026-08-24.
  Коміт: docs: update PROJECT_MAP.md — Board & Task Sharing (US13-US17)
    позначено done після Approve з фіксом reindex-leak

2026-08-24 — Board/task description у FE (boards.description +
  tasks.notes виведені в UI): done, без зміни статусів вузлів
  Підсумок від fullstack-developer + code-reviewer (Approve, коміт
    18fc643):
    - BE: 2000-символьна валідація довжини додана для boards.description
      (раніше не валідувалось) і tasks.notes (нова, ідентична
      normalizeDescription/normalizeNotes у boards.service.js /
      tasks.service.js), нові error-ключі errors.board.descriptionTooLong
      / errors.task.notesTooLong, openapi.yaml оновлено (maxLength 2000
      на обох полях, notes у create/update task body).
    - FE: BoardsPage.jsx — поле опису у формі створення й inline-формі
      перейменування борду, 2-рядковий preview на картці. BoardViewPage.jsx
      — поле опису у формі створення таски, опис борду під заголовком
      Board view. TaskPanel.jsx — нова секція опису (перегляд/редагування,
      дзеркалить існуючий UX title-rename), реюзає колонку notes,
      релейблену "Description" в UI (не нова колонка).
    - Локалізація: повні EN/UK ключі board.create/rename.
      descriptionLabel/descriptionPlaceholder/validation.
      descriptionTooLong, task.create.descriptionLabel/
      descriptionPlaceholder/validation.descriptionTooLong,
      task.notes.label/placeholder/empty/edit/add.
  Позначено done: —  (жоден вузол не змінив клас; boards.description і
    tasks.notes існували в схемі й раніше, ендпоінти вже були done)
  Позначено progress: —
  Додано нові вузли: —  (фіча повністю в межах уже done FE_Boards,
    FE_BoardView, FE_TaskPanel, BE_Boards, BE_Tasks, DB_Boards, DB_Tasks —
    UI/валідаційне розширення вже задокументованих CRUD-ендпоінтів, той
    самий принцип, що й попередні розширення контракту)
  USER_STORIES.md: не оновлено цього разу — жодна US-стрічка не описує
    саме "показати description/notes у FE" як окрему story (US-002/
    US-003/US-006 вже "✅ Готово" і не згадують description в AC);
    залишено на уточнення, чи заводити нову US, перш ніж редагувати
    статуси.
  Коміт: docs: update PROJECT_MAP.md — Board/task description у FE
    задокументовано (без зміни статусів вузлів)
